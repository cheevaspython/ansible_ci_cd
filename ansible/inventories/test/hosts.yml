[swarm_managers]
# Замените на IP вашего Swarm manager ноды для test окружения
# Пример: test-swarm-01 ansible_host=192.168.1.100 ansible_user=admin_remote ansible_port=3322

[swarm_workers]
# Если есть отдельные worker ноды для test
# Пример: test-worker-01 ansible_host=192.168.1.101 ansible_user=admin_remote ansible_port=3322

[test:children]
swarm_managers
swarm_workers
